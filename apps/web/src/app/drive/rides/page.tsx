import { ShellMain } from "@/components/shell/app-shell";
import { DriverRides } from "@/components/partners/driver-rides";

export default function DriveRidesPage() {
  return (
    <ShellMain>
      <DriverRides />
    </ShellMain>
  );
}
