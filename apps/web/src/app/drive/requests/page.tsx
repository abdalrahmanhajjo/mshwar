import { ShellMain } from "@/components/shell/app-shell";
import { DriverInbox } from "@/components/partners/driver-inbox";

export default function DriveRequestsPage() {
  return (
    <ShellMain>
      <DriverInbox />
    </ShellMain>
  );
}
