import { ShellMain } from "@/components/shell/app-shell";
import { RequestInbox } from "@/components/guide/request-inbox";

export default function GuideRequestInboxPage() {
  return (
    <ShellMain>
      <RequestInbox />
    </ShellMain>
  );
}
