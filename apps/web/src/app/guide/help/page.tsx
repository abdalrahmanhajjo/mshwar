import { ShellMain } from "@/components/shell/app-shell";
import { GuideHelp } from "@/components/guide/guide-help";

export default function GuideHelpPage() {
  return (
    <ShellMain>
      <GuideHelp />
    </ShellMain>
  );
}
